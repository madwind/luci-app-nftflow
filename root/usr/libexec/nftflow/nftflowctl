#!/bin/sh
# Thin dispatcher: each subsystem owns its current implementation.

RUNTIME_CTL=/usr/libexec/nftflow/runtime.uc
CONFIG_CTL=/usr/libexec/nftflow/config.uc
FIREWALL_CTL=/usr/libexec/nftflow/firewall-template.uc
ROUTING_CTL=/usr/libexec/nftflow/routing.uc
UPDATE_CTL=/usr/libexec/nftflow/update.uc
ROUTING_SOURCE=/etc/nftflow/routing.conf
ROUTING_DEFAULT=/usr/share/nftflow/defaults/routing.conf

run_ucode() {
    /usr/bin/ucode "$@"
}

if [ "$1" = "run-guarded" ]; then
    shift
    xray_bin="$1"
    config_file="$2"
    asset_dir="$3"
    child_pid=
    stop_requested=0
    failure_message=

    set_state() {
        state_name="$1"
        state_pid="${2:-}"
        state_message="${3:-}"
        run_ucode "$RUNTIME_CTL" state "$state_name" "$state_pid" "$state_message" >/dev/null 2>&1 || true
    }

    terminate_child() {
        [ -z "$child_pid" ] && return 0
        if kill -0 "$child_pid" >/dev/null 2>&1; then
            kill -TERM "$child_pid" >/dev/null 2>&1 || true
            _wait=1
            while [ "$_wait" -le 5 ]; do
                kill -0 "$child_pid" >/dev/null 2>&1 || break
                sleep 1
                _wait=$((_wait + 1))
            done
            kill -0 "$child_pid" >/dev/null 2>&1 && kill -KILL "$child_pid" >/dev/null 2>&1 || true
        fi
        wait "$child_pid" 2>/dev/null || true
    }

    cleanup() {
        trap - EXIT TERM INT
        run_ucode "$FIREWALL_CTL" firewall off >/dev/null 2>&1 || true
        run_ucode "$ROUTING_CTL" route del >/dev/null 2>&1 || true
        terminate_child
        if [ "$stop_requested" = "1" ]; then set_state stopped; else set_state failed "" "${failure_message:-Xray exited unexpectedly}"; fi
    }

    stop_child() {
        stop_requested=1
        set_state stopping "$child_pid" "stop requested"
        terminate_child
        exit 143
    }

    trap cleanup EXIT
    trap stop_child TERM INT
    mkdir -p /var/run/nftflow || exit 1

    XRAY_LOCATION_ASSET="$asset_dir" "$xray_bin" run -format yaml -c "$config_file" &
    child_pid=$!
    set_state starting "$child_pid" "Xray process is starting"
    if ! kill -0 "$child_pid" >/dev/null 2>&1; then
        wait "$child_pid" 2>/dev/null
        child_status=$?
        failure_message='Xray exited during startup'
        set_state failed "$child_pid" "$failure_message"
        [ "$child_status" -ne 0 ] || child_status=1
        exit "$child_status"
    fi

    firewall_output="$(run_ucode "$FIREWALL_CTL" firewall on 2>&1)"; firewall_status=$?
    [ "$firewall_status" -eq 0 ] || logger -t nftflowctl "firewall startup apply failed: ${firewall_output:-unknown error}"

    if [ -r "$ROUTING_SOURCE" ]; then
        routing_output="$(run_ucode "$ROUTING_CTL" route-apply 2>&1)"; routing_status=$?
    elif [ -r "$ROUTING_DEFAULT" ]; then
        routing_output="$(run_ucode "$ROUTING_CTL" routing-apply "$(cat "$ROUTING_DEFAULT")" 2>&1)"; routing_status=$?
    else
        routing_output="missing routing source: $ROUTING_SOURCE and $ROUTING_DEFAULT"; routing_status=1
    fi
    [ "$routing_status" -eq 0 ] || logger -t nftflowctl "routing startup apply failed: ${routing_output:-unknown error}"

    set_state ready "$child_pid" 'Xray process is running'
    wait "$child_pid"
    child_status=$?
    failure_message="Xray exited with status $child_status"
    exit "$child_status"
fi

case "$1" in
    config-read|config-validate|config-validate-file|config-save|config-save-file|config-apply|config-apply-file|config-prepare-file)
        exec /usr/bin/ucode "$CONFIG_CTL" "$@" ;;
    firewall|firewall-read|firewall-validate|firewall-validate-file|firewall-save|firewall-save-file|firewall-apply|firewall-apply-file)
        exec /usr/bin/ucode "$FIREWALL_CTL" "$@" ;;
    route-apply)
        if [ ! -r "$ROUTING_SOURCE" ] && [ -r "$ROUTING_DEFAULT" ]; then exec /usr/bin/ucode "$ROUTING_CTL" routing-apply "$(cat "$ROUTING_DEFAULT")"; fi
        exec /usr/bin/ucode "$ROUTING_CTL" "$@" ;;
    route|routing-read|routing-validate|routing-validate-file|routing-save|routing-save-file|routing-apply|routing-apply-file)
        exec /usr/bin/ucode "$ROUTING_CTL" "$@" ;;
    geo)
        case "${2:-}" in
            status) exec /usr/bin/ucode "$UPDATE_CTL" geo-status ;;
            *) printf '%s\n' '{"ok":false,"error":"unsupported GeoData command"}'; exit 1 ;;
        esac ;;
    status|prepare|cleanup|state|action|reload|service-sync)
        exec /usr/bin/ucode "$RUNTIME_CTL" "$@" ;;
    *)
        printf '%s\n' '{"ok":false,"error":"unsupported nftflowctl command"}'; exit 1 ;;
esac
